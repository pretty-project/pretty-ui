
(ns pretty-forms.smart-form.views)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn view
  ; @param (keyword)(opt) form-id
  ; @param (map) form-props
  ; {:get-fields-f (function)(opt)
  ;  :update-fields-f (function)(opt)}
  [])

; controller functions -> pretty-controls
