
(ns pretty-forms.api
    (:require [pretty-forms.engine.api :as pretty-forms.engine]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (pretty-forms.engine.api/*)
(def validate-form!  pretty-forms.engine/validate-form!)
(def validate-input! pretty-forms.engine/validate-input!)
