
(ns templates.item-preview.api
    (:require [engines.item-preview.api]
              [templates.item-preview.body.views :as body.views]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; templates.item-preview.body.views
(def body body.views/body)
