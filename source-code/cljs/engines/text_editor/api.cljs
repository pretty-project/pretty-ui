
(ns engines.text-editor.api
    (:require [engines.text-editor.views :as views]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; engines.text-editor.views
(def body views/body)
