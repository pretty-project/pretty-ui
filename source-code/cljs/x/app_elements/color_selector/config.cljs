
;; -- Namespace ---------------------------------------------------------------
;; ----------------------------------------------------------------------------

(ns x.app-elements.color-selector.config)



;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @constant (metamorphic-content)
(def DEFAULT-NO-COLORS-LABEL :add-color!)

; @constant (strings in vector)
(def DEFAULT-OPTIONS ["var( --soft-blue )"
                      "var( --soft-purple )"
                      "var( --soft-green )"
                      "var( --soft-red )"])
