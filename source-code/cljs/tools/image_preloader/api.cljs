
(ns tools.image-preloader.api
    (:require [tools.image-preloader.views :as views]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; tools.image-preloader.views
(def component views/component)
