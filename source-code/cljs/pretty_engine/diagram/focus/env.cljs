
(ns pretty-engine.diagram.focus.env
    (:require [pretty-engine.element.focus.env]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (pretty-engine.element.focus.env/*)
(def diagram-focused? pretty-engine.element.focus.env/element-focused?)
