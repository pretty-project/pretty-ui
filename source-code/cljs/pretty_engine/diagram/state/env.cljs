
(ns pretty-engine.diagram.state.env
    (:require [pretty-engine.element.state.env]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (pretty-engine.element.state.env/*)
(def get-diagram-state pretty-engine.element.state.env/get-element-state)
