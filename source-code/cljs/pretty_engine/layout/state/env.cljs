
(ns pretty-engine.layout.state.env
    (:require [pretty-engine.element.state.env]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (pretty-engine.element.state.env/*)
(def get-layout-state pretty-engine.element.state.env/get-element-state)
