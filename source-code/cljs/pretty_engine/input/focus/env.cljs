
(ns pretty-engine.input.focus.env
    (:require [pretty-engine.element.focus.env]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (pretty-engine.element.focus.env/*)
(def input-focused? pretty-engine.element.focus.env/element-focused?)
