
(ns pretty-engine.input.state.env
    (:require [pretty-engine.element.state.env]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (pretty-engine.element.state.env/*)
(def get-input-state pretty-engine.element.state.env/get-element-state)
