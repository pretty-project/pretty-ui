
(ns components.vector-item-list.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn list-props-prototype
  ; @param (map) list-props
  ;
  ; @return (map)
  [_])
