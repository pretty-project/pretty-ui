
(ns components.list-item-drag-handle.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn handle-props-prototype
  ; @param (map) handle-props
  ;
  ; @return (map)
  [_])
