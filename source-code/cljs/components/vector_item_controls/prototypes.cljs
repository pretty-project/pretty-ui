
(ns components.vector-item-controls.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn controls-props-prototype
  ; @param (map) controls-props
  ;
  ; @return (map)
  [_])
