
(ns components.consent-dialog.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn dialog-props-prototype
  ; @param (map) dialog-props
  ;
  ; @return (map)
  [_])
