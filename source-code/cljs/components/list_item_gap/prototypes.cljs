
(ns components.list-item-gap.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn gap-props-prototype
  ; @param (map) row-props
  ;
  ; @return (map)
  [_])
