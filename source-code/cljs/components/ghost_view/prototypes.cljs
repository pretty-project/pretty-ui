
(ns components.ghost-view.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn view-props-prototype
  ; @param (map) view-props
  ;
  ; @return (map)
  [_])
