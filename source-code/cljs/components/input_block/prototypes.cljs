
(ns components.input-block.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn block-props-prototype
  ; @param (map) block-props
  ;
  ; @return (map)
  [_])
