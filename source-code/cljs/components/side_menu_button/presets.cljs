
(ns components.side-menu-button.presets)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @constant (map)
(def BUTTON-PROPS-PRESETS
     {:active {:fill-color  :muted
               :hover-color :muted
               :font-weight :bold}})
