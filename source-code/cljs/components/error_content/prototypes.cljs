
(ns components.error-content.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn content-props-prototype
  ; @param (map) content-props
  ;
  ; @return (map)
  [_])
