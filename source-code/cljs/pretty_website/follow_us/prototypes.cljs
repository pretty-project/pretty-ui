
(ns pretty-website.follow-us.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn component-props-prototype
  ; @ignore
  ;
  ; @param (map) component-props
  ;
  ; @return (map)
  [_])
