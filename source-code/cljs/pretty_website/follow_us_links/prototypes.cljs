
(ns pretty-website.follow-us-links.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn links-props-prototype
  ; @ignore
  ;
  ; @param (map) links-props
  ;
  ; @return (map)
  [_])
