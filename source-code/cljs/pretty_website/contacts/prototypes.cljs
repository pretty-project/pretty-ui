
(ns pretty-website.contacts.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn contacts-props-prototype
  ; @ignore
  ;
  ; @param (map) contacts-props
  ;
  ; @return (map)
  [_])
