
(ns pretty-shapes.api
    (:require [pretty-shapes.sector.views :as sector.views]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (*/view)
(def sector sector.views/view)
