
(ns pretty-inputs.input.views
    (:require [pretty-elements.element.views]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (pretty-elements.element.views/*)
(def input-label pretty-elements.element.views/element-label)
