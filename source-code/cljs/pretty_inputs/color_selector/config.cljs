
(ns pretty-inputs.color-selector.config)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @constant (strings in vector)
(def DEFAULT-OPTIONS ["var( --soft-blue )"
                      "var( --soft-purple )"
                      "var( --soft-green )"
                      "var( --soft-red )"])
