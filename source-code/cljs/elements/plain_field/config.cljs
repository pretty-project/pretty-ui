
(ns elements.plain-field.config)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @ignore
;
; @constant (ms)
; The timeout after the last key pressed to fire the :on-type-ended event.
(def TYPE-ENDED-AFTER 450)
