
(ns pretty-elements.stepper.prototypes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn stepper-props-prototype
  ; @ignore
  ;
  ; @param (map) stepper-props
  ;
  ; @return (map)
  [_])
