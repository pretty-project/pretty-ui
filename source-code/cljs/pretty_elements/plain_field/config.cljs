
(ns pretty-elements.plain-field.config)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @ignore
;
; @constant (ms)
; How much time has to elapse after the last key pressed to fire the ':on-type-ended' event.
(def TYPE-ENDED-AFTER 450)
